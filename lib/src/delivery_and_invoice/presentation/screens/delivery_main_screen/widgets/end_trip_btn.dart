import 'package:flutter/material.dart';

class EndTripBtn extends StatelessWidget {
  const EndTripBtn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}