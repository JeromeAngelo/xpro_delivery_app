import 'package:flutter/material.dart';

class FinalReturnScreen extends StatelessWidget {
  const FinalReturnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text('Final Return Screen'),),);
  }
}